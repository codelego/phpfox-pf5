<?php
if (isset($heading)) {
    $form->setParam('title', $heading);
}
if (isset($render)) {
    $form->setRender($render);
}else{
    $form->setRender('form_panel_horizontal');
}
echo $form->render();